# ============================================================================
# TaskFlow Backend Environment Configuration
# ============================================================================
# Copy this file to .env and update with your values
# REQUIRED variables MUST be set or the application will refuse to start
# ============================================================================

# Server Configuration
# Optional - defaults to 8080
PORT=8080

# Optional - use "release" for production
GIN_MODE=debug

# ============================================================================
# REQUIRED: Database Configuration
# ============================================================================
# The application will NOT start without a valid DATABASE_URL
# For Supabase: Get this from your Supabase project settings -> Database -> Connection string
# Format: postgres://user:password@host:port/database?sslmode=require
DATABASE_URL=your_database_url_here

# ============================================================================
# REQUIRED: JWT Configuration
# ============================================================================
# The application will NOT start without JWT_SECRET
# IMPORTANT: Generate a strong random secret (minimum 32 characters)
# DO NOT use the example value below - generate your own!
#
# To generate a secure secret, use one of these commands:
#   openssl rand -base64 32
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
JWT_SECRET=CHANGE_ME_TO_A_SECURE_RANDOM_STRING_MINIMUM_32_CHARS

# Optional - Token expiry time in hours (default: 24)
JWT_EXPIRY_HOURS=24

# ============================================================================
# Optional: Rate Limiting
# ============================================================================
# Maximum requests per minute per IP address (default: 100)
RATE_LIMIT_REQUESTS_PER_MINUTE=100

# ============================================================================
# Optional: CORS Configuration
# ============================================================================
# Comma-separated list of allowed origins (default: http://localhost:3000)
# Production example: https://app.example.com,https://www.example.com
ALLOWED_ORIGINS=http://localhost:3000
